.pio/
.vscode/
!scope-clock.code-workspace
*.hex
*.elf
*.bin
.DS_Store

# Secrets. This repo is public — .env holds real Wi-Fi credentials and must
# never be committed. The tracked template is .env.example.
.env
.env.*
!.env.example

# Host harness build output. The SOURCE is committed (see CLAUDE.md — a harness
# that lives only on one machine evaporates), the binaries are not.
tools/hostsim/sizeface
tools/hostsim/thumbs
tools/websim/websim
tools/websim/names.h
*.dSYM/
